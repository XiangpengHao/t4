[package]
name = "t4"
version = "0.1.0"
edition = "2024"

[workspace]
members = [".", "fuzz"]

[features]
default = []
shuttle = ["dep:shuttle"]

[dependencies]
libc = "0.2"
io-uring = "0.7"
shuttle = { version = "0.8", optional = true }

[dev-dependencies]
pollster = "0.4"
tempfile = "3"
